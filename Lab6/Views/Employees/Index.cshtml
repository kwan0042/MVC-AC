@model IList<Lab6.Models.DataAccess.Employee>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                Employee Name
            </th>
            <th>
                Network ID
            </th>
            <th>
                Job Title
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@*@for(int i = 0; i <Model.Count() ; i++)
{
        <tr>
            <td>
                @Model.Name)
            </td>
            <td>
                @Model[i].Employee.UserName
            </td>
            <td>
                @Model[i].Role.Role1)
            </td>

            <td>
                <a asp-action="Edit" asp-route-id="@Model[i].Employee.Id">Edit</a> |
                <a asp-action="Delete" asp-route-id="@Model[i].Employee.Id">Delete</a>
            </td>
        </tr>
}*@

@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.UserName)
            </td>
            <td>
                    @for (int i=0; i<item.EmployeeRole.Count;i++)
                    {
                        
                        if (item.EmployeeRole.Count>1)
                        {
                            <span>@item.EmployeeRole[i].Role.Role1</span>
                            if (i<item.EmployeeRole.Count-1){
                            <span>,</span>
                            }
                        
                        }
                        else
                        {
                            <span>@item.EmployeeRole[i].Role.Role1</span>
                        }
                    }
               
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
